import styled from 'styled-components';

export const PlayButton = styled.div`
    display: flex;
    position: fixed;
    bottom: 0;
    left: 0;
    width: 100%;
`;

export const PlayMusicTypes = styled.div``;

export const PlayMusicStatus = styled.div``;
